import { fetchReviews } from '@/lib/data'
import React from 'react'

export default async function CardReviews({
    recipeId
}: {
    recipeId: string
}) {

    return (
        <div>
        </div>
    )
}
